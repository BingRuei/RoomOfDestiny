package com.app.ray.roomofdestiny;

import android.os.Bundle;
import android.app.Activity;

public class Server3ModelList extends Activity {

    /**
     * Choosing one of models,
     * and conveying the sockets from last page to next page
     * **/

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_server3_model_list);
    }

}
